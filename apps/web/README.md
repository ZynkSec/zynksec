# @zynksec/web

Zynksec dashboard — **placeholder only** in Phase 0 Week 1.

The package manifest declares Next.js 14 + React 18 + Tailwind so
`pnpm install` resolves them and the workspace wiring is exercised.
No `app/` or `next.config.mjs` ships until Week 2, when the real
scan-results UI lands alongside the DB layer.
