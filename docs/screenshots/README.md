# Screenshots

Images referenced from the top-level `README.md` live here.

- `glitchtip-capture.png` — awaiting manual capture. Shows a captured
  exception in GlitchTip's event detail view with the
  `correlation_id` tag visible in the right-hand sidebar. The
  screenshot proves that a deliberate test exception thrown from
  the api surfaces in the self-hosted error tracker with full
  context including the request's correlation_id.
